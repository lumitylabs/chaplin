# === Clerk Api Key ===
VITE_CLERK_PUBLISHABLE_KEY={api_key}

# === Backend ===
# Vercel deploy url or local url, ending with api route
VITE_APP_API_BASE_URL="https://{your_app}.vercel.app/api"

# === Cloudflare Worker (text-to-image) ===
# Create an account → select text-to-image template
# Use code from backend/external/workers_code_exemple.js
VITE_IMAGE_WORKER_URL="https://text-to-image-template.{your_account}.workers.dev"